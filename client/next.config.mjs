const images = {
  images: {
    remotePatterns: [
      {
        protocol: "http",
        hostname: "localhost",
        port: "3001",
        pathname: "/api/images/generated_image.png",
      },
    ],
  },
};

export default images;
